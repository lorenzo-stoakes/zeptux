#pragma once

// We have to specify these basic fundamentals as we no longer have the
// assistance of any standard headers.
#define true (1)
#define false (0)
#define NULL ((void *)0)
#define UINT64_MAX_CHARS (20)
